// Function Declartion

function naveen(){
    console.log("bit harder to read");
    
}
naveen();

// Function Declartion with parameter

function name(undifined) {
    console.log("hello " + undifined + " |" );
    
}
name("sanjay");     //arguments

///default parameter

function math(x,y, z) {
    console.log(x + y + z);
    
}
math(5, 10, 30);